version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 22.2
    commands:
      - echo "Installing Backend Dependencies..."
      - cd backend
      - npm install

  build:
    commands:
      - echo "Zipping Backend Application..."
      - zip -r backend.zip .

artifacts:
  files:
    - backend.zip
